[project]
name = "icsport"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "icalendar>=6.3.1",
    "polars>=1.30.0",
]
